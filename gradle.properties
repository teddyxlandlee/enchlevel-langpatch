# Done to increase the memory available to Gradle.
org.gradle.jvmargs=-Xmx1G
loader_version=0.18.0
# Mod Properties
mod_version=3.8.1
maven_group=xland.mcmod
archives_base_name=enchlevel-langpatch

changelog=3.8.1: make telemetry more robust. Fixes #9.\n\n\
  Telemetry v2 is introduced, including current_hooks \
  (functional) and all_hooks (optional).
# stable, beta, alpha
release_type=beta

template_patch_number=3
