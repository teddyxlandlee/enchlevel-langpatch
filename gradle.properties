# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx1G
# Fabric Properties
# check these on https://modmuss50.me/fabric.html
#minecraft_version=1.16.5
#yarn_mappings=1.16.5+build.10
loader_version=0.13.0
# Mod Properties
    # Mod version must be SemVer-friendly
mod_version=3.1.0
maven_group=xland.mcmod
archives_base_name=enchlevel-langpatch

changelog=Supports language key "langpatch.conf.enchantment.override" and "langpatch.conf.potion.override"\
   (default to `false`).When set to `true`, LangPatch-generated format will override language files (e.g.\
  you can see "Sharpness 5" without removing the item "enchantment.level.5" in language files).\n\n\
  Technical update:\nAdded `EnchantmentLevelLanguagePatch.withFallback` for 1.19.4+ patches.
release_type=stable
