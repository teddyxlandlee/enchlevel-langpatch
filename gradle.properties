# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx1G
loader_version=0.18.0
# Mod Properties
mod_version=3.6.0
maven_group=xland.mcmod
archives_base_name=enchlevel-langpatch

changelog=3.6.0:\n\n- Make `get*HooksContxt() stable`\n-    \
  Refactor registry internals\n- Remove `unmodifiableMap()` \
  calls when `storage` is already unmodifiable (default     \
  vanilla behavior).
# stable, beta, alpha
release_type=stable

template_patch_number=3
